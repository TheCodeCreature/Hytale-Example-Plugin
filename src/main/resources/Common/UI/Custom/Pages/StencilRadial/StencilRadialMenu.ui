// Stencil Radial Menu — page root
// Server appends Pages/StencilRadial/Components/SegmentButton.ui instances into #Segments during build()
// Server positions each segment absolutely via Left/Top on the appended child

$C = "../../Common.ui";
$B = "../../Styles/Buttons.ui";
$L = "../../Styles/Labels.ui";
$BG = "../../Styles/Backgrounds.ui";

Group {
    Anchor: (Full: 0);
    LayoutMode: Full;
    Background: $BG.@DimOverlay;

    // Centered container for the radial layout
    Group #RadialRoot {
        Anchor: (Width: 400, Height: 400);
        LayoutMode: Full;

        // Container for dynamically appended segments
        Group #Segments {
            Anchor: (Full: 0);
            LayoutMode: Full;
        }

        // Container for cost icon slots (outer ring, shown on hover)
        Group #CostSlots {
            Anchor: (Full: 0);
            LayoutMode: Full;
        }

        // Center hub — background panel with close, pagination, and delete
        Group #CenterHub {
            Anchor: (Width: 130, Height: 120, Left: 135, Top: 140);
            LayoutMode: Top;
            Padding: (Left: 5, Right: 5, Top: 6, Bottom: 6);

            // Close button (X) at top
            TextButton #CloseBtn {
                Anchor: (Width: 30, Height: 30);
                Text: "X";
                Style: $B.@DestructiveButtonStyle;
            }

            // Page navigation row
            Group #CenterNav {
                Anchor: (Width: 120, Height: 36);
                LayoutMode: Left;
                Padding: (Full: 2);

                TextButton #PrevBtn {
                    Anchor: (Width: 32, Height: 32);
                    Text: "<";
                    Style: $B.@NavButtonStyle;
                }

                Label #PageLabel {
                    Anchor: (Width: 48, Height: 32);
                    Style: $L.@PageIndicatorLabelStyle;
                    Text: "1 / 1";
                }

                TextButton #NextBtn {
                    Anchor: (Width: 32, Height: 32);
                    Text: ">";
                    Style: $B.@NavButtonStyle;
                }
            }

            // Delete button (trash icon)
            Group #DeleteBtn {
                Anchor: (Width: 32, Height: 32);
                Background: $BG.@TrashIcon;

                TextButton #DeleteBtnHit {
                    Text: "";
                    Anchor: (Full: 0);
                }
            }
        }
    }
}
