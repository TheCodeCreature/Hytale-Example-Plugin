// Blueprint Bench — single recipe icon cell
// ItemSlotButton provides Activating events + auto item tooltips
// Server sets #CellIcon.ItemId via: #RecipeGrid[i] #CellIcon.ItemId

ItemSlotButton {
    Anchor: (Width: 80, Height: 80);

    ItemIcon #CellIcon {
        Anchor: (Full: 4);
    }
}
