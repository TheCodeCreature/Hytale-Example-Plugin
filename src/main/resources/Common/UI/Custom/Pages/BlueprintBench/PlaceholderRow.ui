// PlaceholderRow — one row per placeholder item in the hotbar
// Server appends into #PlaceholderList and sets properties via indexed selectors

$C = "../../Common.ui";

@RowLabelStyle = LabelStyle(
    ...$C.@DefaultLabelStyle,
    FontSize: 11, TextColor: #96a9be,
    HorizontalAlignment: Start, VerticalAlignment: Center, Wrap: true
);
@CancelLabelStyle = LabelStyle(
    ...$C.@DefaultLabelStyle,
    FontSize: 16, RenderBold: true,
    HorizontalAlignment: Center, VerticalAlignment: Center
);

Group {
    LayoutMode: Left;
    Anchor: (Height: 64, Left: 0, Right: 0);

    // Slot number label
    Label #RowSlotLabel {
        Text: "";
        Anchor: (Width: 16);
        Style: @RowLabelStyle;
    }

    // Drop target — 1-slot ItemGrid for arming
    Group {
        Anchor: (Width: 56, Height: 56);
        LayoutMode: Center;
        Background: "../../Common/InputBox.png";

        Group{
            LayoutMode: Center;
            ItemGrid #RowInputSlot {
                FlexWeight: 1;
                SlotsPerRow: 1;
                DisplayItemQuantity: false;
                RenderItemQualityBackground: false;
                Style: (SlotSize: 48, SlotIconSize: 48, SlotSpacing: 0);
            }
        }
    }

    Group { Anchor: (Width: 6); }

    // Block name + clear button stacked Horizontally
    Group {
        FlexWeight: 1;
        LayoutMode: Left;
    
        Label #RowBlockName {
            Text: "";
            Anchor: (Height: 28);
            Style: @RowLabelStyle;
        }

        Group{
            FlexWeight:1;
            LayoutMode: Right;
            $C.@CancelButton #RowClearBtn{
                Anchor: (Width: 24, Height: 24);
                Visible: false;
                Label {
                    Text: "x";
                    Style: @CancelLabelStyle;
                }
            }
        }
    }
}
