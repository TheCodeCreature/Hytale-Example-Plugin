// Shared radial segment button — framed icon with label (matches bench #OutputFrame pattern)
// Appended N times via cmd.append("#Segments", "Common/Components/SegmentButton.ui")
// Java selectors: #Segments[N] #SegFrame, #Segments[N] #Icon, #Segments[N] #Dim, #Segments[N] #Btn, #Segments[N] #SegLabelText

$B = "../../Styles/Buttons.ui";
$O = "../../Styles/Overlays.ui";
$BG = "../../Styles/Backgrounds.ui";

Group #SegRoot {
    Anchor: (Width: 116, Height: 126);
    LayoutMode: Full;

    Group #SegFrame {
        Anchor: (Width: 96, Height: 96, Left: 10, Top: 0);
        Background: $BG.@OutputSlotFrame;

        ItemIcon #Icon {
            Anchor: (Full: 4);
            ShowItemTooltip: false;
        }

        Group #Dim {
            Anchor: (Full: 2);
            Background: $BG.@DimOverlay;
            Visible: false;
        }

        TextButton #Btn {
            Text: "";
            Anchor: (Full: 0);
            Style: $B.@TransparentButtonStyle;
        }
    }

    Group #SegLabel {
        Anchor: (Width: 116, Height: 28, Left: 0, Top: 97);
        Label #SegLabelText {
            Text: "";
            Anchor: (Full: 0);
            Style: $O.@OverlayLabelStyle;
        }
    }
}
