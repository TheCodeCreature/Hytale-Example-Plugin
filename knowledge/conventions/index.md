---
type: index
title: Conventions
description: Hard, repo-specific coding rules and patterns for the Hytale Example Plugin.
---

# Conventions

Empty for now. Add a `type: rule` note here per hard, repo-specific convention as they come up, and link it from this index.

This folder follows [Open Knowledge Format](https://okf.dev) strictly: plain markdown links only (no wikilinks), and `type` is the only required frontmatter field. See the global vault's graph-conventions note (`~/.claude/knowledge/meta/graph-conventions.md`, personal to this machine) for the full frontmatter schema and tagging rules if you have access to it; otherwise a plain `type: rule` note with a short body is enough.
